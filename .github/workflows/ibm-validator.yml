name: validate

on: [push]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: actions/setup-node@v2
        with:
          node-version: '10'
      - name: Install IBM OpenAPI Validator
        run: npm install -g ibm-openapi-validator
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run validator
        run: lint-openapi Master-OB-OpenAPI.json -c .ibm-validator-config -e

  swagger_editor_validator_remote:
    runs-on: ubuntu-latest
    name: Swagger Editor Validator Remote
    steps:
      - uses: actions/checkout@v2
      - name: Validate OB OpenAPI
        uses: char0n/swagger-editor-validate@master
        with:
          definition-file: Master-OB-OpenAPI.json
